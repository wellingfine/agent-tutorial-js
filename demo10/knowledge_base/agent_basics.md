# Agent 基础

Agent 不是简单的一问一答程序。普通聊天机器人通常只根据当前用户输入生成回复，而 Agent 会围绕一个目标持续工作。

一个最小 Agent 通常包含四个部分：

- 模型：负责理解用户目标、推理下一步、生成回复。
- 消息：保存系统提示词、用户消息、助手消息和工具结果。
- 工具：让模型能够执行外部动作，例如读文件、写文件、查询数据库。
- 运行时：负责把模型、消息、工具和状态组织成一个循环。

在教程前几课里，我们从最简单的 API 调用开始，逐步加入多轮对话、工具调用、状态维护、ReAct 循环和 workflow 编排。
